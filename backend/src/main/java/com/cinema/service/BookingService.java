package com.cinema.service;

public class BookingService {


    //this class should handle the checkout/place-order business logic.
    // Instead of creating a Booking object manually with lots of setters, use the new
    // BookingBuilder (design pattern implemented) to construct the Booking, then save it with bookingRepository.save(booking)

}
